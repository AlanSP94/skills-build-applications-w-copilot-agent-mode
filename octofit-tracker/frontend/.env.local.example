# Define the GitHub Codespace name used by the frontend to build the backend API URL.
# Example: if your Codespace name is `myspace`, the app will request
# https://myspace-8000.app.github.dev/api/
VITE_CODESPACE_NAME=your-codespace-name
